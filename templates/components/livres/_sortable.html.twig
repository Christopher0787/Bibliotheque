<div class="d-flex align-item-center gap-2">
	{{ knp_pagination_sortable(livres, 'Date', 'a.createdAt', {class: 'btn btn-outline-primary'} ) }}
	{{ knp_pagination_sortable(livres, 'Titre', 'a.title', {class: 'btn btn-outline-primary'} ) }}
</div>
