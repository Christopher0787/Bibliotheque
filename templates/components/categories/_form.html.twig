{{ form_start(form, {attr: {class: 'card p-3 w-50 mx-auto'}}) }}
{{ form_widget(form) }}

<button type="submit" class="btn btn-primary">{{ buttonLabel|default('Envoyer') }}</button>
{{ form_end(form) }}

<a href="{{ path('admin_categorie_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
